1='cd -'			# cd -1 in history
2='cd -2'			# cd -2 in history
3='cd -3'			# cd -3 in history
4='cd -4'			# cd -4 in history
5='cd -5'			# cd -5 in history
6='cd -6'			# cd -6 in history
7='cd -7'			# cd -7 in history
8='cd -8'			# cd -8 in history
9='cd -9'			# cd -9 in history
YT="youtube-viewer -C -D"	# Launch youtube-viewer
cvpn="sudo protonvpn c -f --p2p"	# Connect protonvpn
cv="sudo protonvpn c -f --p2p"	# Connect protonvpn
dvpn="sudo protonvpn d"	# Disconnect protonvpn
dv="sudo protonvpn d"	# Disconnect protonvpn
svpn="sudo protonvpn s"	# Check protonvpn status
sv="sudo protonvpn s"	# Check protonvpn status
e=vim			# Launch vim
ffmpeg='ffmpeg -hide_banner'	# Launch ffmpeg
g=git			# Git
ga='git add'			# Git add
gaa='git add --all'	# Git add everything
gb='git branch'
gc='git commit -v'
gp='git push'
gpd='git push --dry-run'
ka=killall			# killall +PID
md='mkdir -p'			# Create directory
mkd='mkdir -pv'			# Create directory
p='sudo pacman'			# Package manager usually -Suy
rd=rmdir			# Remove directory
sdn='sudo shutdown -h now'	# Shutdown computer
v=vim			# Launch vim
yt='youtube-dl'			# Youtube downloader best quality no error
yte='yt --exec "mpv $$"'	# Download and view youtube video
yta='yt -x -f bestaudio/best'	# Youtube downloader audio only
YT="youtube-viewer -C --player=mpv"	# Youtube viewer no safe search
z=zathura			# Zathura PDF viewer
